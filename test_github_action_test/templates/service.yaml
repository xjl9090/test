apiVersion: v1
kind: Service
metadata:
  name: {{ include "test_github_action_test.fullname" . }}
  labels:
    {{- include "test_github_action_test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test_github_action_test.selectorLabels" . | nindent 4 }}
