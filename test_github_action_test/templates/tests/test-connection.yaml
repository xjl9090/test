apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test_github_action_test.fullname" . }}-test-connection"
  labels:
    {{- include "test_github_action_test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test_github_action_test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
